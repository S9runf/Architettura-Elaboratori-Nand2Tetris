#include <stdio.h>
#include <string.h>
#include <stdlib.h>


char * next_command(FILE *a);
char command_type(char * a);

int is_in(char * a, char x);
char * symbol(char * a);
char * destm (char *a);
char * compm (char *a);
char * jumpm (char *a);
